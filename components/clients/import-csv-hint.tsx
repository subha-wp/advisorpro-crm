export function ImportCsvHint() {
  return (
    <p className="text-sm text-muted-foreground">
      CSV columns: name, mobile, email, address, tags (comma-separated). Import UI coming next.
    </p>
  )
}
